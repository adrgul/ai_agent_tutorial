# Qdrant
QDRANT_URL=http://127.0.0.1:6333
VECTOR_SIZE=1536

# Embeddings (HF2 ingesthez kell majd)
OPENAI_API_KEY=CHANGEME
EMBEDDING_MODEL=text-embedding-3-small

# Chunking (ingest.py fogja használni)
CHUNK_SIZE=600
CHUNK_OVERLAP=80

# Retrieval (search.py-hoz)
TOP_K=6
MIN_SCORE=0.25

# Domain collectionök
DOMAINS=hr,compliance,it,ops,general
